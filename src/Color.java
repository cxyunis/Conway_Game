public enum Color {
    /*
    * color for the cells - default color for empty cell
    * playe can choose color
    * board will need the color to fill the grid
    * need to define color codes for each of enum color
    * */
    GREY, BLUE, GREEN, YELLOW, ORANGE
}
